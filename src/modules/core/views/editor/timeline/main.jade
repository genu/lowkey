div(style="display: none;")
    video(ng-repeat="layer in timeline.layers", id="{{layer.media.id}}", height="200px", width="200px", controls)
        source(ng-repeat="source in layer.media.sources", src="{{source.src}}", type="{{source.type}}")

.panel-body(ng-class="{dropzone: timeline.isAddingMedia}", style='height:400px;', ng-drop='true', ng-drop-success='timeline.onAddMedia($data)')
    div.message(ng-if="timeline.isAddingMedia") Drop media here
    vis-timeline#test1(ng-if='!timeline.isAddingMedia', ng-show='true', data='timelineData', options='timelineOptions', events='graphEvents')